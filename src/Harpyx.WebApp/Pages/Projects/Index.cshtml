@page
@model Harpyx.WebApp.Pages.Projects.IndexModel
@{
    ViewData["Title"] = Localizer["PageTitle"].Value;
}

<div class="space-y-6">
    <div class="card bg-base-100 shadow">
        <div class="card-body">
            <div class="flex items-center gap-4">
                <h1 class="text-2xl font-semibold">@Localizer["Heading"]</h1>
                <div class="ml-auto">
                    <a class="btn btn-primary btn-sm" asp-page="/Projects/Edit">
                        <iconify-icon icon="mdi:plus"></iconify-icon>
                        @Localizer["AddProject"]
                    </a>
                </div>
            </div>
            <hr />
            <p class="text-sm text-base-content/70">
                @Localizer["Intro"]
            </p>
        </div>
    </div>

    <div class="card bg-base-100 shadow">
        <div class="card-body space-y-4">
            <form method="get" class="flex flex-wrap items-end gap-3">
                <div class="form-control w-full max-w-xs">
                    <label class="label"><span class="label-text">@Localizer["Tenant"]</span></label>
                    <select asp-for="FilterTenantId" class="select select-bordered select-sm w-full" id="filter-tenant">
                        <option value="">@Localizer["AllTenants"]</option>
                        @foreach (var option in Model.TenantOptions)
                        {
                            <option value="@option.Value" selected="@option.Selected">@option.Text</option>
                        }
                    </select>
                </div>

                <div class="form-control w-full max-w-xs">
                    <label class="label"><span class="label-text">@Localizer["Workspace"]</span></label>
                    <select asp-for="FilterWorkspaceId" class="select select-bordered select-sm w-full">
                        <option value="">@Localizer["AllWorkspaces"]</option>
                        @foreach (var option in Model.WorkspaceOptions)
                        {
                            <option value="@option.Value" selected="@option.Selected">@option.Text</option>
                        }
                    </select>
                </div>

                <div class="form-control w-full max-w-xs">
                    <label class="label"><span class="label-text">@Localizer["SortBy"]</span></label>
                    <select asp-for="SortBy" class="select select-bordered select-sm w-full">
                        <option value="name" selected="@(Model.SortBy == "name")">@Localizer["SortName"]</option>
                        <option value="activity" selected="@(Model.SortBy == "activity")">@Localizer["SortLastActivity"]</option>
                        <option value="docs" selected="@(Model.SortBy == "docs")">@Localizer["SortDocuments"]</option>
                        <option value="chat" selected="@(Model.SortBy == "chat")">@Localizer["SortChatMessages"]</option>
                        <option value="status" selected="@(Model.SortBy == "status")">@Localizer["SortStatus"]</option>
                    </select>
                </div>

                <button type="submit" class="btn btn-primary btn-sm">
                    <iconify-icon icon="mdi:filter-variant"></iconify-icon>
                    @Localizer["Apply"]
                </button>

                @if (Model.FilterTenantId is not null || Model.FilterWorkspaceId is not null || Model.SortBy != "name")
                {
                    <a asp-page="/Projects/Index" class="btn btn-ghost btn-sm">
                        <iconify-icon icon="mdi:filter-remove"></iconify-icon>
                        @Localizer["Reset"]
                    </a>
                }
            </form>
        </div>
    </div>

    <div class="card bg-base-100 shadow">
        <div class="card-body">
            @if (Model.Rows.Count == 0)
            {
                <div class="rounded-lg border border-base-200 bg-base-100 p-4 text-sm text-base-content/70">
                    @if (Model.FilterTenantId is not null || Model.FilterWorkspaceId is not null)
                    {
                        <span>@Localizer["NoProjectsForFilters"]</span>
                    }
                    else
                    {
                        <span>@Localizer["NoProjectsPrefix"]
                            <a asp-page="/Workspaces/Index" class="link link-hover font-semibold">@Localizer["WorkspacesLink"]</a>
                            @Localizer["NoProjectsSuffix"]
                        </span>
                    }
                </div>
            }
            else
            {
                <div class="overflow-x-auto">
                    <table class="table table-sm">
                        <thead>
                            <tr>
                                <th>@Localizer["Project"]</th>
                                <th>@Localizer["Workspace"]</th>
                                <th class="text-center">@Localizer["Docs"]</th>
                                <th class="text-center">@Localizer["Chat"]</th>
                                <th>@Localizer["LastActivity"]</th>
                                <th>@Localizer["Status"]</th>
                                <th class="text-right">@Localizer["Actions"]</th>
                            </tr>
                        </thead>
                        <tbody>
                        @foreach (var row in Model.Rows)
                        {
                            <tr>
                                <td>
                                    <div>
                                        <a class="link link-hover link-primary font-medium"
                                           asp-page="/Projects/Details"
                                           asp-route-id="@row.Id"
                                           title="@(Localizer["OpenProjectTitle", row.Name])">
                                            @row.Name
                                        </a>
                                        @if (!string.IsNullOrWhiteSpace(row.Description))
                                        {
                                            <div class="text-xs text-base-content/60 truncate max-w-xs">@row.Description</div>
                                        }
                                    </div>
                                </td>
                                <td>
                                    <div class="text-sm">@row.WorkspaceName</div>
                                    <div class="text-xs text-base-content/60">@row.TenantName</div>
                                </td>
                                <td class="text-center">
                                    @if (row.TotalDocs == 0)
                                    {
                                        <span class="text-base-content/40">-</span>
                                    }
                                    else
                                    {
                                        <span class="font-medium" title="@(Localizer["IndexedDocsTitle", row.IndexedDocs, row.TotalDocs])">
                                            @row.IndexedDocs/@row.TotalDocs
                                        </span>
                                        @if (row.FailedDocs > 0)
                                        {
                                            <span class="text-error text-xs" title="@(Localizer["FailedDocsTitle", row.FailedDocs])">
                                                (@row.FailedDocs
                                                <iconify-icon icon="mdi:alert-circle" class="align-middle"></iconify-icon>)
                                            </span>
                                        }
                                    }
                                </td>
                                <td class="text-center">
                                    @if (row.ChatMessages == 0)
                                    {
                                        <span class="text-base-content/40">-</span>
                                    }
                                    else
                                    {
                                        <span>@row.ChatMessages</span>
                                    }
                                </td>
                                <td>
                                    <span class="text-sm" title="@row.LastActivity?.ToString("yyyy-MM-dd HH:mm")">
                                        @Harpyx.WebApp.Pages.Projects.IndexModel.FormatTimeAgo(row.LastActivity)
                                    </span>
                                </td>
                                <td>
                                    @{
                                        var badgeClass = row.Status switch
                                        {
                                            "All indexed" => "badge-success",
                                            "Processing" => "badge-info",
                                            "Has failures" => "badge-error",
                                            "Empty" => "badge-ghost",
                                            _ => "badge-warning"
                                        };
                                    }
                                    <span class="badge badge-sm @badgeClass">@row.Status</span>
                                </td>
                                <td class="text-right">
                                    <div class="inline-flex items-center gap-1">
                                        <a class="btn btn-ghost btn-xs btn-square"
                                           asp-page="/Projects/Details" asp-route-id="@row.Id"
                                           title="@(Localizer["Open"])" aria-label="@(Localizer["Open"])">
                                            <iconify-icon icon="mdi:view-dashboard-outline"></iconify-icon>
                                        </a>
                                        <a class="btn btn-ghost btn-xs btn-square"
                                           asp-page="/Projects/Edit" asp-route-id="@row.Id"
                                           title="@(Localizer["Edit"])" aria-label="@(Localizer["Edit"])">
                                            <iconify-icon icon="mdi:pencil"></iconify-icon>
                                        </a>
                                        <form method="post" asp-page-handler="Delete" class="inline">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="id" value="@row.Id" />
                                            <button type="submit"
                                                    class="btn btn-ghost btn-xs btn-square text-error"
                                                    title="@(Localizer["Delete"])" aria-label="@(Localizer["Delete"])">
                                                <iconify-icon icon="mdi:delete"></iconify-icon>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                        </tbody>
                    </table>
                </div>

                <div class="text-xs text-base-content/60 mt-2">
                    @(Model.Rows.Count == 1
                        ? Localizer["OneProject"]
                        : Localizer["ProjectCount", Model.Rows.Count])
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        (function () {
            var tenantSelect = document.getElementById('filter-tenant');
            if (tenantSelect) {
                tenantSelect.addEventListener('change', function () {
                    if (!tenantSelect.value) {
                        return;
                    }
                    var workspaceSelect = document.querySelector('[name="FilterWorkspaceId"]');
                    if (workspaceSelect) {
                        workspaceSelect.value = '';
                    }
                });
            }
        })();
    </script>
}
